# Database Configuration for MySQL Workbench
spring.datasource.url=jdbc:mysql://localhost:3306/care_sync_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Prabhakar$66

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Remove the dialect line or update it
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Security (for development)
spring.security.filter.order=10

# Logging
logging.level.com.medtech.patientportal=DEBUG